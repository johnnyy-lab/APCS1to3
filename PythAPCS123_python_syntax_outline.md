# Python 程式設計從 APCS 實作 1 級到 3 級：語法結構與真題特訓對照表

- **來源教材**：《PythAPCS123：Python 程式設計從 APCS 實作 1 級到 3 級》
- **教材作者**：吳邦一
- **整理說明**：本表將全書知識重構梳理為 **14 個核心章節與 104 個小節**。
  - **第 1 至 13 章（語法與基礎演算法，共 85 節）**：嚴格落實「零提前依賴」原則，確保每章僅使用該章及先前已授之語法，無任何後續語法偷跑，讓完全零基礎初學者循序漸進扎實學習。
  - **第 14 章（APCS 實作真題解題特訓，共 19 節）**：完整收錄講義第 11 章與第 15 章全數 APCS 實作真題（實作二級分 9 題、三級分 10 題），每節專注講解一題，循序「分析題意 ➔ 拆解技巧 ➔ 演算法邏輯 ➔ 學生獨立撰寫」，建立完整解題實戰力。

---

| 章名稱 | 節名稱 | 內容簡介 | 小節規劃 | 對應講義章節與頁數 | 練習檔是否生成 | 簡報檔是否生成 |
| :--- | :--- | :--- | :--- | :--- | :---: | :---: |
| **第一章 變數、賦值與基本語法慣例** | 1.1 變數命名規則與記憶體參照概念 | 說明變數由英數底線組成、非數字開頭及不能撞關鍵字等規則；變數代表記憶體位址的專屬代稱。 | 1.1.1 變數基本概念——貼上名牌的神奇置物箱<br>1.1.2 記憶體參照與找不到箱子的錯誤（NameError）<br>1.1.3 變數命名規則一——名牌只能使用英文字母、數字與底線（_）<br>1.1.4 變數命名規則二——頭號禁忌：開頭絕對不能是數字！<br>1.1.5 變數命名規則三——大小寫大不同（超敏銳的辨識眼）<br>1.1.6 變數命名規則四——避開魔法咒語（保留關鍵字與內建名稱） | 第 1 章 變數與資料存取（第 4 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 1.2 等號賦值與運算順序 | 介紹賦值運作順序（先算等號右側再存入左側變數），說明左側必須為變數位置。 | 1.2.1 等號不是等於！認識單向賦值輸送帶（Assignment）<br>1.2.2 賦值的黃金順序——「先算右邊，再存左邊」<br>1.2.3 等號左側的嚴格鐵律——左邊只能是「單一變數箱子」！<br>1.2.4 舊的不去新的不來——變數賦值的覆蓋（Overwrite）特性<br>1.2.5 自己算自己——右側引用舊值自我更新（`x = x + 1` 的魔法） | 第 1 章 變數與資料存取（第 4–5 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 1.3 多變數同時賦值與變數交換（Swap） | 利用右側先求值特性，以 `a, b = b, a` 進行兩數交換，以及多變數輪替賦值（`a, b, c = b, c, a`）。 | 1.3.1 多變數同時賦值——雙手齊發的打包與解包<br>1.3.2 等號兩側天平必須對稱——個數不符的 ValueError 警報<br>1.3.3 傳統變數交換的慘痛悲劇——為什麼需要第三個空杯子 temp？<br>1.3.4 Python 專屬的瞬間乾坤大挪移——`a, b = b, a` 變數交換<br>1.3.5 三人傳球大輪替——多變數循環交換（Cyclic Swap） | 第 1 章（第 5 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 1.4 運算後賦值（複合賦值運算子） | 介紹累加與運算更新的精簡寫法：`+=`, `-=`, `*=`, `//=`, `%=`, `**=`。 | 1.4.1 複合加法賦值（+=）——神奇的快速加料按鈕<br>1.4.2 複合減法賦值（-=）——受到攻擊扣血與右側先算鐵則<br>1.4.3 複合乘法賦值（*=）——攻擊力爆發與倍數升級<br>1.4.4 複合整除與取餘數（//=, %=）——位數縮小與循環分堆<br>1.4.5 複合次方賦值（**=）——威力驚人的幾何連乘 | 第 2 章 運算後賦值（第 6–7 頁）、第 17.4 節（第 81 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 1.5 單行多指令（分號）與註解（#） | 使用分號 `;` 在單行書寫多個指令的語法；井字號 `#` 後方為編譯器忽略的單行註解。 | 1.5.1 單行註解（#）——電腦的隱形斗篷與工程師的隨身筆記<br>1.5.2 單行多指令（;）——省空間的車廂連接器<br>1.5.3 分號的優雅紀律——過猶不及與一組關聯原則<br>1.5.4 註解的除錯魔法——暫停嫌疑犯的「程式碼封印術」 | 第 1 章（第 4, 5 頁）、第 3 章（第 9 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 1.6 縮排規範與程式區塊 | 說明指令前方不可任意加空白，Python 依賴縮排（Tab 或 4 個空白）界定程式區塊結構。 | 1.6.1 行首嚴禁任意留白——抓出出列站錯的 IndentationError<br>1.6.2 縮排界定程式區塊——「母雞帶小鴨」的層級從屬概念<br>1.6.3 空格與 Tab 絕不混用——守護建築結構的 TabError 警告<br>1.6.4 優雅的呼吸留白——中間與後方的美化空白規範 | 第 1 章（第 4 頁）、第 8 章（第 20 頁） | ✅ 已生成 | ⏳ 製作中 |
| **第二章 資料型態與數值 / 位元運算** | 2.1 數值型態分類（整數與浮點數） | 區分整數 int 與浮點數 float，強調自動裁判中輸出 `4.0` 與 `4` 會因格式不符判定錯誤。 | 2.1.1 數字世界的兩大宗族——整數積木（int）與浮點果汁（float）<br>2.1.2 查驗身分的照妖鏡——認識內建型態檢查工具 type()<br>2.1.3 染紅效應——整數與浮點數混合運算自動晉升為浮點數<br>2.1.4 APCS 考場的致命失分陷阱——4.0 與 4 大不相同（Wrong Answer 的眼淚） | 第 2 章（第 6 頁）、第 3 章 浮點數（第 8 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 2.2 整數基本算術運算與優先級 | 加減乘運算、先乘除後加減原則，以及多重小括號 `(...)` 在複雜算式中的使用。 | 2.2.1 整數加法與減法（+、-）——數值增減與負數運算<br>2.2.2 整數乘法（*）與代數陷阱——星號連乘與嚴禁省略乘號<br>2.2.3 運算子優先級鐵則——先乘後加減與由左至右結合性<br>2.2.4 運算順序的王牌指揮官——小括號 (...) 搶先執行權<br>2.2.5 多重巢狀小括號與括號大忌——由內而外層層拆解 | 第 2 章（第 6 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 2.3 整數除法商數（//）與取餘數（%） | `//` 整除取商與 `%` 模除取餘；應用於純數值位數拆解（個位數 `x%10`、右移一位 `x//10`、各十百位數提取）。 | 2.3.1 整數除法雙斜線（//）——只留整數商數的無情去尾刀<br>2.3.2 模除取餘數百分比（%）——撿回落單零頭的餘數探測器<br>2.3.3 奇偶數判定與分組循環——餘數的週期魔法<br>2.3.4 神奇位數拆解術——取出個位數（% 10）與右移剝皮（// 10）<br>2.3.5 時間與單位的複合進位換算——商與餘數的黃金雙重奏 | 第 2 章（第 6 頁）、第 10 章（第 30 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 2.4 次方與開根號運算（**） | 次方運算語法 `a**b`，以及用 `a**0.5` 開根號運算結果轉為浮點數的特性與轉型注意。 | 2.4.1 次方運算雙星號（**）與脫字符號（^）的巨大陷阱<br>2.4.2 次方的超高優先級與負數括號陷阱<br>2.4.3 用 a ** 0.5 進行開根號計算與浮點型態轉變<br>2.4.4 APCS 實戰質數判定加速——開根號取整 int(n ** 0.5) 技巧 | 第 2 章（第 6 頁）、第 9 章（第 25 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 2.5 浮點數除法（/）與精度限制 | 單斜線除法必產生 float，解析電腦以二進位表示十進位小數時的無限循環與精度誤差。 | 2.5.1 單斜線除法（/）——必定產生浮點數的魔法轉身<br>2.5.2 電腦底層的二進位秘密——0.1 + 0.2 != 0.3 的精度誤差真相<br>2.5.3 APCS 浮點數相等的安全比對法——容許誤差（Epsilon）與 abs()<br>2.5.4 浮點數輸出修剪術——round() 四捨五入與 f-string 格式化 | 第 3 章 浮點數（第 8 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 2.6 進位制常數表示法（0b, 0x） | 介紹二進位前綴 `0b`（如 `0b10101`）與十六進位前綴 `0x`（如 `0x41`）的數值宣告。 | 2.6.1 電腦的原生母語——二進位常數表示法（0b）<br>2.6.2 資訊科學的高速密碼——十六進位常數表示法（0x）<br>2.6.3 十進位與進位制的雙向翻譯機——bin() 與 hex() 內建轉換函數<br>2.6.4 APCS 實戰視野：進位制在狀態開關與位元遮罩（Bitmask）的解題先導 | 第 3 章（第 10 頁）、第 17.1 節（第 78 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 2.7 位元運算子與解題加速 | 說明位元 AND `&`、OR <code>&#124;</code>、XOR `^`、左移 `<<`、右移 `>>`，並示範以 `x&1` 取代模除加速判斷奇偶數。 | 2.7.1 位元 AND（&）與超光速奇偶判斷（x & 1）<br>2.7.2 位元 OR（&#124;）與狀態開關強制啟用<br>2.7.3 位元 XOR（^）與開關翻轉／相同自消魔法<br>2.7.4 位元左移（<<）與右移（>>）——二進位滑梯與乘除倍增 | 第 17.1 節 位元運算（第 78 頁） | ✅ 已生成 | ⏳ 製作中 |
| **第三章 型態轉換與內建數學函數** | 3.1 數值與文字強制轉型（int, float, str） | 詳解 `int()` 轉整數無條件捨去小數、`float()` 轉小數、`str()` 轉文字以利數值與字串相互轉換。 | 3.1.1 int() 浮點數轉整數——無條件向零截斷（Truncation）<br>3.1.2 int() 純整數字串轉整數——打破型態隔閡與致命的 ValueError 陷阱<br>3.1.3 float() 轉型為浮點數——打造小數刻度與兩段式字串小數轉整數法<br>3.1.4 str() 轉型為文字字串——安全無縫拼接與格式輸出<br>3.1.5 數值與文字強制轉型實務綜合應用——建立商品標籤與計費字串輸出 | 第 3 章 轉換函數（第 9 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 3.2 字元與 ASCII 互轉（ord, chr） | `ord()` 字元轉編碼、`chr()` 編碼轉字元；利用大寫與小寫連續且差值為 32 的性質進行轉換。 | 3.2.1 ord() 字元身分證——探訪 ASCII 編碼與連續性特徵<br>3.2.3 經典差值 32（0x20）——純手工大小寫無痛轉換<br>3.2.4 字母相對索引 ord(c) - ord('A')——26 個字母計數器起手式<br>3.2.5 凱撒密碼位移——字元循環輪替加密小魔法 | 第 3 章（第 9–10 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 3.3 極值比較函數（max, min） | 支援多引數數值比大小（兩數、三數至多數），解析巢狀合成函數由內而外求值順序，以及三數取中間值技巧。 | 3.3.1 max() 與 min() 多參數比大小——誰是擂台冠軍與墊底守門員<br>3.3.2 巢狀合成函數運算——由內而外的同心圓求值順序 | 第 2 章（第 7 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 3.4 絕對值計算（abs） | 利用 `abs()` 求得數值差值或數線距離，以純數學一行化計算取代正負向差值判斷程式碼。 | 3.4.1 認識 abs() 絕對值函數——數線上的無向距離守護者<br>3.4.2 兩數無向差值計算——一行求得差距，取代正負號手動調整<br>3.4.3 曼哈頓距離（兩點網格距離）計算——abs(x1 - x2) + abs(y1 - y2)<br>3.4.4 兩組總和差距計算實務——abs(odd_sum - even_sum) 的純數學化身 | 第 2 章（第 7 頁） | ✅ 已生成 | ⏳ 製作中 |
| **第四章 標準輸出入（I/O）與測資處理技巧** | 4.1 標準輸出 print() 基本語法 | `print()` 基本輸出指令，多項目輸出時預設以單一空白間隔並自動換行。 | 4.1.1 print() 的初體驗——電腦的發聲大喇叭與字串輸出<br>4.1.2 print() 輸出數值與即時算術——讓喇叭播報運算結果<br>4.1.3 逗號 `,` 串聯多個項目——預設單一空格隔開的神奇魔術<br>4.1.5 不帶引數的 print()——空白行與段落視覺排版術 | 第 1 章 輸出函數 print()（第 4 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 4.2 print() 分隔符號參數 sep | 利用 `sep` 自訂各項目間的分隔字元，如設定逗號 `sep=','` 或冒號比分 `sep=':'`。 | 4.2.1 認識分隔參數 sep——揭開預設半形空格的秘密<br>4.2.2 自訂常用標點符號分隔——逗號、連字號與斜線<br>4.2.3 消除間隔的無縫緊密黏合——空字串 sep=''<br>4.2.4 趣味多字元與文字字串分隔——流程箭頭與對戰排版<br>4.2.5 特殊跳脫分隔符號——換行符號 sep='\n' 與跳格 sep='\t' | 第 1 章（第 4 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 4.3 print() 結尾符號參數 end | 利用 `end=''` 取消行尾自動換行，以利在後續迴圈或連續指令中接續輸出。 | 4.3.1 認識結尾參數 end——揭開自動換行 \n 的秘密<br>4.3.2 取消換行緊密相連——空字串 end='' 接續輸出<br>4.3.3 自訂結尾間隔符號——空格 end=' ' 與標點符號收尾<br>4.3.4 結尾的雙重換行與段落拉開——end='\n\n' 大段落排版術<br>4.3.5 綜合雙劍合璧——sep 與 end 同時登場的精密排版術 | 第 9 章（第 29 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 4.4 變數與運算式綜合輸出 | 在 `print()` 中混合輸出字串提示、變數值與即時算式結果，掌握純文字與數值排版技巧。 | 4.4.1 字串標籤與變數混合輸出——變數名牌 vs 字串引號的本質區別<br>4.4.2 在 print() 中即時運算——算式先求值後輸出的精簡美學<br>4.4.3 消除文字與數值突兀間隔——利用 sep='' 實現中文字詞與單位無縫貼合<br>4.4.4 連續跨行與接續排版——結合 end 參數建構連貫算式與計算歷程<br>4.4.5 實戰全能儀表板——變數、即時運算、sep 與 end 的終極融合 | 第 1 章（第 4–5 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 4.5 標準輸入 input() 基本讀取 | input() 讀入整行字串，搭配 int(input()) 轉型，提醒線上評判環境（OJ）中嚴禁輸出提示文字。 | 4.5.1 input() 的本質——電腦的靈敏耳朵與「輸入一律為字串」的鐵律<br>4.5.2 字串轉整數 int(input())——破解 "10" + "20" = "1020" 陷阱，解鎖真正整數計算<br>4.5.3 小數輸入 float(input())——精準量測身高體重與實數幾何計算<br>4.5.4 循序多次調用 input() 讀入多行單一資料——分行讀入矩形長寬與商品收據<br>4.5.5 競技程式 APCS / OJ 生死線——嚴禁在 input() 括號內放入提示文字的「純淨讀入」哲學 | 第 4 章（第 11 頁）、第 5 章（第 14 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 4.6 單行多數值切割與映射（split, map） | `input().split()` 空白切分字串，搭配 `map(int, ...)` 一次讀入多個整數至個別變數。 | 4.6.1 字串的魔法剪刀——`split()` 空白切割與多變數同時接收<br>4.6.2 自動化加工輸送帶——`map(int, ...)` 一次將多個字串變整數<br>4.6.3 單行讀入三個及以上整數——三數運算、極值與全距實戰<br>4.6.4 單行多浮點數映射——`map(float, ...)` 與精準實數運算<br>4.6.5 APCS / OJ 競技實戰——一行流解析多數值與易錯陷阱防禦 | 第 4 章（第 11 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 4.7 連續多行固定筆數讀取 | 連續調用多次 `input()` 循序讀入多行資料至個別獨立變數中進行運算。 | 4.7.1 分行循序進水閘門——連續多次調用 `input()` 循序讀入多行整數<br>4.7.2 異質型態分行讀入——文字、整數與浮點數的多行混合資料卡<br>4.7.3 多行混合單行多值——「單值行」與「多值行」交替讀取的複合架構<br>4.7.4 幾何多點與雙對數值分行讀取——平面二維座標與曼哈頓距離實戰<br>4.7.5 APCS / OJ 固定行數題型實戰——純淨多行讀取與競賽常見致命陷阱防禦 | 第 4 章（第 11 頁） | ✅ 已生成 | ⏳ 製作中 |
| **第五章 邏輯表示式、布林值與條件分支** | 5.1 比較運算子與連續比較 | 包含 `==`, `!=`, `<`, `<=`, `>`, `>=`，以及 Python 特有的連續比較式（如 `1 < x < 9`）。 | 5.1.1 數值大小比較——大於（`>`）、小於（`<`）、大於等於（`>=`）、小於等於（`<=`）<br>5.1.2 嚴格相等與不相等——雙等號（`==`）與驚嘆號等號（`!=`）<br>5.1.3 運算式與運算優先級——先算術運算，再進行大小比較<br>5.1.4 文字字串的比較——字典序（Lexicographical Order）與大小寫 ASCII<br>5.1.5 Python 獨家黑科技——連續比較式（Chained Comparisons，如 `1 < x < 9`） | 第 7 章 邏輯運算（第 17 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 5.2 邏輯運算子（and, or, not） | 組合條件之真值表邏輯：`and`（且）、`or`（或）、`not`（非）之優先級與運作。 | 5.2.1 邏輯交集運算子：`and`（且）——兩全其美與嚴格門檻<br>5.2.2 邏輯聯集運算子：`or`（或）——暢通無阻與單一達標<br>5.2.3 邏輯否定運算子：`not`（非）——顛倒真假的黑白濾鏡<br>5.2.4 複合邏輯與優先級鐵律：`not` > `and` > `or`<br>5.2.5 邏輯代數實戰：互斥或（XOR，Exclusive OR）的純邏輯構建 | 第 7 章（第 17–18 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 5.3 布林型態（bool）與真假值規則 | 布林值 `True` 與 `False`；數值 0 為假、非 0 為真；空字串為假、非空字串為真。 | 5.3.1 布林型態本質——True 與 False 的身世之謎與整數繼承特性<br>5.3.2 數值的真假值規則——非零為真、零為假<br>5.3.3 文字字串的真假值規則——真空為假、有字為真<br>5.3.4 bool() 函數強制轉型——探索萬物的真假值本質<br>5.3.5 布林值數值化運算黑科技——免條件分支的指標計數神技 | 第 7 章（第 17 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 5.4 笛摩根定律的邏輯改寫 | 解說口語否定與數理邏輯差異，推導 `not(a and b)` 等價於 `(not a) or (not b)` 之改寫法。 | 5.4.1 口語否定與數理邏輯陷阱——「非（A 且 B）」的思維迷宮<br>5.4.2 笛摩根第一定律——not (A and B) 等價於 (not A) or (not B)<br>5.4.3 笛摩根第二定律——not (A or B) 等價於 (not A) and (not B)<br>5.4.4 比較運算子的對偶反轉——徹底消除比較式外層的 not<br>5.4.5 複合條件化簡實戰——將晦澀否定句改寫為明快正向邏輯 | 第 7 章 笛摩根定律（第 18 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 5.5 分支控制結構（if, if-else, if-elif-else） | 介紹單行 if、完整分支選擇與 `elif` 互斥結構，強調冒號 `:` 與縮排一致性。 | 5.5.1 單向選擇 `if` 指令——冒號與縮排程式區塊的誕生<br>5.5.2 雙向互斥選擇 `if-else`——非此即彼的命運分水嶺<br>5.5.3 多向互斥選擇 `if-elif-else`——階梯式分級審查<br>5.5.4 獨立多重 `if` vs 互斥 `if-elif`——破解初學者最常犯的覆蓋天坑<br>5.5.5 單行 if 簡寫語法——俐落優雅的競技程式快速寫法 | 第 8 章 分支選擇指令（第 19–21 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 5.6 巢狀 if 與短路求值（Short-circuit） | 介紹巢狀分支層次；解析 `and` 前項為假則略過後項之特性，示範如除數防零檢查 `b != 0 and a / b > 1`。 | 5.6.1 巢狀分支結構——多層次縮排與條件俄羅斯套娃<br>5.6.2 巢狀 if 與 and 複合條件——雙向轉換與可讀性權衡<br>5.6.3 and 短路求值機制——除數防零檢查（b != 0 and a / b > 1）<br>5.6.4 or 短路求值機制——前真則後略與預設備援防呆<br>5.6.5 APCS 邊界條件防崩潰雙重防線——先合法驗證，再執行運算 | 第 8 章（第 21 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 5.7 旗標變數（Flag）與狀態控制 | 透過事先建立布林旗標（如 `found = False`），在多條件判斷後統籌決定最終單一輸出。 | 5.7.1 什麼是旗標變數——程式記憶狀態的儀表板指示燈<br>5.7.2 旗標的生命週期——歸零初始化、條件觸發與狀態鎖定<br>5.7.3 單一出口原則——以旗標統籌多分支最終輸出<br>5.7.4 多旗標狀態組合——多項指標聯合會審<br>5.7.5 APCS 實作經典模式——c461 前哨戰：IMPOSSIBLE 無解防禦輸出 | 第 8 章（第 21 頁） | ✅ 已生成 | ⏳ 製作中 |
| **第六章 迴圈結構與控制流程** | 6.1 計數迴圈（for 搭配 range） | 說明 `range(n)`、左閉右開 `range(a, b)` 以及含步進值（含負步進倒數）之等差數列走訪。 | 6.1.1 為什麼需要迴圈與 for 迴圈基礎結構——告別人工複製貼上<br>6.1.2 單一參數 range(n)——從 0 開始到 n-1 的 n 次規律點名<br>6.1.3 雙參數 range(start, stop)——「左閉右開（含頭不含尾）」的區間走訪<br>6.1.4 三參數正步進值 range(start, stop, step)——等差數列跳步走訪<br>6.1.5 負步進值倒數計時 range(start, stop, -step)——倒著數的等差走訪與防空轉陷阱 | 第 9 章 控制程式流程—for 迴圈（第 23–24 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 6.2 迴圈累加器與計數器模式 | 以變數歸零初始化，在迴圈中逐回合更新累加（如 1 加到 n 總和、符合條件次數計數）。 | 6.2.1 累加器模式（Accumulator）基礎——小豬撲滿的「歸零」與「逐回合投入」<br>6.2.2 計數器模式（Counter）基礎——特定事件發生的「打勾計次機」<br>6.2.3 結合使用者輸入的動態累加——連續讀入 n 筆測資的流式計算<br>6.2.4 累加與計數的協同作戰——條件加總、求平均值與防除零防禦<br>6.2.5 迴圈動態極值維護——擂台盟主挑戰模式與常數空間串流求極值 | 第 9 章（第 24 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 6.3 迴圈中斷與跳步（break, continue） | `break` 立刻跳離整個迴圈；`continue` 跳過當前回合剩餘指令，進入下一輪。 | 6.3.1 `break` 基礎：緊急煞車踏板——條件滿足立即跳離整個迴圈<br>6.3.2 `continue` 基礎：跨欄跳步踏板——跳過當前回合剩餘指令，直奔下一輪<br>6.3.3 `break` vs `continue` 深度對決——「提早收工」與「跳過換下一輪」的本質差異<br>6.3.4 `break` 搭配布林旗標（Flag）的高效搜尋模式——「找到就停」的線性搜尋與質數檢驗<br>6.3.5 `continue` 在串流數據清洗與安全過濾中的實戰應用——挑選符合條件的純淨數據 | 第 9 章（第 26–27 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 6.4 條件迴圈（while 迴圈） | 條件滿足即反覆執行；應用於未知次數迭代（輾轉相除求 GCD、因數分解、數值逐位拆解）。 | 6.4.1 `while` 迴圈基礎：條件哨兵與計數迭代——只要條件為真就絕不停步<br>6.4.2 純數值位數逐位拆解：`// 10` 與 `% 10` 的剝皮術——未知位數的整數分析<br>6.4.3 未知次數的折半與逼近：考拉茲猜想（3n + 1）與對數級縮小<br>6.4.4 古希臘智慧：歐幾里得輾轉相除法（求最大公因數 GCD）<br>6.4.5 `while True` 搭配 `break`：動態事件驅動與哨兵終止模式 | 第 10 章 控制程式流程—while 迴圈（第 30–32 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 6.5 雙重與多重巢狀迴圈 | 外層每輪帶動內層整輪運作；應用於九九乘法表排版、星號幾何圖形及雙層數值枚舉。 | 6.5.1 雙重迴圈基礎機制：時鐘的分針與時針——外層帶動內層的維度展開<br>6.5.2 九九乘法表排版大作戰：`end` 參數與換行節奏的精準掌控<br>6.5.3 直角三角形與幾何圖形：內層邊界與外層變數的動態綁定<br>6.5.4 雙層窮舉與數值枚舉搜尋（Brute-Force Enumeration）<br>6.5.5 巢狀迴圈中的 `break` 範圍陷阱與多層跳出技巧 | 第 9 章 雙重迴圈（第 28–29 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 6.6 迴圈控制變數的常見陷阱 | 解析在 range 內竄改迴圈變數無效、倒數步進遺漏邊界（off-by-one），以及無窮迴圈條件設計錯誤。 | 6.6.1 `for` 迴圈內竄改計數變數無效的「自動校正陷阱」<br>6.6.2 左閉右開與負步進倒數的「差一邊界陷阱（Off-by-one Error）」<br>6.6.3 迴圈結束後計數變數的「殘留值與生命週期陷阱」<br>6.6.4 `while` 迴圈步伐迷航與條件永遠為真的「無窮死結陷阱」<br>6.6.5 巢狀迴圈控制變數的「命名覆蓋（Shadowing）與歸零位置陷阱」 | 第 17.1 節 for 迴圈注意事項（第 77 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 6.7 未知行數與檔案結尾測資讀取（while 搭配 EOF 處理） | 說明線上題庫未知行數的測資概念，運用 while True 搭配 break / 例外處理進行 EOF 迴圈中斷讀取技巧。 | 6.7.1 什麼是 EOF？線上題庫未知筆數測資的真實面貌與鍵盤模擬<br>6.7.2 `while True` 搭配 `try ... except EOFError: break` 黃金標準模板<br>6.7.3 EOF 模式下的同行多數值讀取：`map()` 與 `split()` 的安全協同<br>6.7.4 複合異常捕捉：空白行與格式雜訊的防禦（`except (EOFError, ValueError)`）<br>6.7.5 競技程式實戰：未知筆數數據流串流處理（Streaming）大總結 | 第 10 章（第 30 頁）、第 17.1 節 EOF 結尾的測資（第 77–78 頁） | ✅ 已生成 | ⏳ 製作中 |
| **第七章 字串（String）特性與序列操作** | 7.1 字串表示法與跳脫字元 | 單雙引號的使用規範，以及 `\n`（換行）、`\t`（跳格）、`\\`（印出反斜線）等跳脫字符處理。 | 7.1.1 單引號 ' ' 與雙引號 " " 的表示法與成對配對原則<br>7.1.2 引號巢狀包夾技巧（外單內雙、外雙內單）<br>7.1.3 常用跳脫字元：\n（換行）與 \t（水平跳格 Tab）<br>7.1.4 跳脫自身與引號：\\、\' 與 \" 的特殊防護<br>7.1.5 三引號 ''' 與 """ 多行文字與原始排版 | 第 1 章（第 4 頁）、第 3 章（第 9 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 7.2 字串序列操作（串接與重複） | 說明字串加法 `+` 為拼接、字串乘整數 `*` 為重複序列（如 `'*'*5` 快速造字串）。 | 7.2.1 字串加法運算子（+）：串接與文字拼接<br>7.2.2 多變數與連續串接：累加與複合賦值（+=）<br>7.2.3 字串乘法運算子（*）：整數倍率重複造字<br>7.2.4 運算優先順序與小括號：加法與乘法的混用<br>7.2.5 迴圈與字串運算實戰：動態圖形與文字累加構造 | 第 3 章（第 9 頁）、第 9 章（第 29 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 7.3 字串索引、長度與走訪 | 取用長度 `len(s)`、單字元正負索引存取；以 `for ch in s:` 走訪字元，強調字串不可變性（Immutable）。 | 7.3.1 字串長度函數 len() 與空字串邊界<br>7.3.2 正向索引（Positive Indexing）與 IndexError 超界防禦<br>7.3.3 負向索引（Negative Indexing）：倒數定位技巧<br>7.3.4 字串不可變性（Immutable）：就地竄改的致命陷阱<br>7.3.5 兩大字串走訪模式：字元走訪 vs 索引走訪 | 第 9 章（第 26 頁）、第 12 章（第 45 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 7.4 字串切片與反轉技巧（[::-1]） | 詳解左閉右開切片語法，示範利用負步進值 `step=-1` 進行高效字串反轉判斷迴文。 | 7.4.1 字串基本切片：s[start:stop] 左閉右開區間與超界容錯<br>7.4.2 省略預設邊界：s[:stop] 與 s[start:] 俐落截取<br>7.4.3 步進值切片：s[start:stop:step] 等差跳步抽取<br>7.4.4 負步進值與字串反轉神技：s[::-1] 深度剖析<br>7.4.5 經典解題實戰：迴文檢測（Palindrome）與對稱字串 | 第 12 章（第 45, 46 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 7.5 字串常用方法（split, count, in） | 說明 `s.split()` 空白拆詞、`s.count()` 統計字元頻率，以及用 `in` 檢查子字串存在性。 | 7.5.1 子字串成員檢查：in 與 not in 運算子<br>7.5.2 字元與字詞頻率統計：.count() 方法與非重疊特性<br>7.5.3 句子拆詞利器：.split() 空白切割與詞彙走訪<br>7.5.4 尋找子字串位置：.find() 安全查詢與 -1 回傳機制<br>7.5.5 大小寫轉換比對：.lower() 與 .upper() 規範化對照 | 第 4 章（第 11 頁）、第 9 章（第 26 頁）、第 12 章（第 45–46 頁） | ✅ 已生成 | ⏳ 製作中 |
| **第八章 一維串列（List）核心操作與列表生成式** | 8.1 串列建立、正負索引與解包輸出（print(*a)） | 串列宣告、正負索引定位（最後一個為 `-1`）與元素更新；以 `print(*a)` 快速解包以空白隔開輸出所有項目。 | 8.1.1 串列概念、中括號建立與異質容器特性<br>8.1.2 串列正負向索引：精準讀取各隔間物品<br>8.1.3 串列可變性（Mutable）：就地替換元素（a[i] = val）<br>8.1.4 固定長度預設值初始化：[0] * n 快速造表<br>8.1.5 競技輸出必殺技：星號解包輸出 print(*a) | 第 4 章（第 12 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 8.2 串列切片語法與切片賦值 | 說明左閉右開切片；展示將切片賦予新串列可同時修改長度與內容，以及 `a[:] = []` 清空法。 | 8.2.1 串列切片提取：a[start:stop] 左閉右開子串列<br>8.2.2 省略起終點與步進跳步切片：a[:k]、a[k:] 與 a[::2]<br>8.2.3 串列反轉切片：a[::-1] 與逆序呈現<br>8.2.4 串列切片就地替換賦值：a[start:stop] = [...] 動態伸縮<br>8.2.5 切片清空與區間刪除：a[:] = [] 的就地洗牌術 | 第 12 章 list slicing（第 44–45 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 8.3 串列常用內建方法（append, pop, insert 等） | 尾端增刪 `append()`, `pop()`；插入 `insert()`；刪除指定值 `remove()` 與就地反轉 `reverse()`。 | 8.3.1 尾端動態追加：.append(x) 方法與迴圈收集模式<br>8.3.2 元素彈出與提取：.pop() 尾端彈出與指定索引彈出<br>8.3.3 指定位置插隊：.insert(index, x) 方法<br>8.3.4 指定數值精確刪除：.remove(x) 方法與 ValueError 防禦<br>8.3.5 串列就地反轉：.reverse() 方法與切片對比 | 第 12 章（第 44 頁）、第 17.3 節（第 79–81 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 8.4 串列統計與極值運算（sum, max, min, len） | `sum()` 快速求總和與平均、`max()` 與 `min()` 整條串列取極值，以及防禦空串列安全守則。 | 8.4.1 串列元素長度總數：len(a) 與空串列守護法則<br>8.4.2 串列快速總和：sum(a) 與平均值計算技巧<br>8.4.3 串列全局極值查詢：max(a) 與 min(a)<br>8.4.4 全距極差運算與空串列安全防禦（max - min 避坑）<br>8.4.5 實戰統計分析：去頭去尾裁判評分法（扣除最高最低分） | 第 4 章（第 13 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 8.5 串列走訪與成員查詢（for, index, in） | 以 `for x in a:` 直接取值、以 `for i in range(len(a)):` 配合索引更新，以及用 `in` 防範不存在錯誤。 | 8.5.1 直接元素走訪：`for item in my_list:`<br>8.5.2 索引走訪與元素更新：`for i in range(len(my_list)):`<br>8.5.3 雙軌資訊走訪：同時掌握索引與數值<br>8.5.4 成員查詢與存在性判斷：`in` 與 `not in` 運算子<br>8.5.5 定位元素索引：`index()` 方法與安全查詢模式 | 第 12 章（第 45 頁）、第 17.3 節（第 80 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 8.6 串列參照與淺拷貝（copy, [:]） | 剖析 `b = a` 僅為指標參照之陷阱；說明複製獨立副本必須使用 `a.copy()` 或 `a[:]`。 | 8.6.1 變數賦值的大陷阱：`b = a` 只是共用同一張標籤<br>8.6.2 身分證驗證：`id()` 函式與 `is` 運算子<br>8.6.3 製作真正的獨立副本：`copy()` 方法<br>8.6.4 切片複製經典技法：`b = a[:]`<br>8.6.5 淺拷貝（Shallow Copy）的核心本質與防禦實踐 | 第 4 章（第 13 頁）、第 17.3 節（第 80 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 8.7 列表生成式與動態輸入（List Comprehension） | 以 `[int(x) for x in input().split()]` 讀入同行不定量數值，以及帶條件過濾 `if` 與多重巢狀之生成式。 | 8.7.1 列表生成式基礎語法：`[運算式 for 變數 in 迭代對象]`<br>8.7.2 列表生成式搭配條件篩選：`[運算式 for 變數 in 迭代對象 if 條件]`<br>8.7.3 APCS 必備同行數值動態輸入：`[int(x) for x in input().split()]`<br>8.7.4 雙重分支與三元運算生成式：`[值A if 條件 else 值B for ...]`<br>8.7.5 列表生成式實戰綜合特訓 | 第 4 章（第 11 頁）、第 12 章（第 45 頁） | ✅ 已生成 | ⏳ 製作中 |
| **第九章 二維陣列（2D Array / List of Lists）與網格模擬** | 9.1 二維陣列概念與座標元素存取 | 介紹 `list of list` 結構與尺寸量測（R, C），辨析整列提取與單格存取型態差異，以先列後行 `grid[r][c]` 精準定位九宮格與原地修改。 | 9.1.1 二維陣列物理模型：串列的串列（List of Lists）<br>9.1.2 網格尺寸掌握：列數 `R = len(grid)` 與行數 `C = len(grid[0])`<br>9.1.3 整列提取 vs 單格存取：`grid[r]` 與 `grid[r][c]` 的型態與運算差異<br>9.1.4 座標定位存取：`grid[r][c]` 先列後行絕對守則與九宮格打靶<br>9.1.5 網格元素原地修改：`grid[r][c] = 新數值`<br>9.1.6 負向索引定位與維度混淆陷阱防禦 | 第 13 章 list of list 實作二維陣列（第 47 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 9.2 二維陣列動態輸入讀取與解包輸出 | 從傳統 for 迴圈 append 逐列讀入平緩過渡至一行巢狀生成式，掌握 `print(*row)` 解包、格式化排版與字元網格 `list()` 讀入。 | 9.2.1 APCS 題目典型格式：多行數值測資輸入流程剖析<br>9.2.2 傳統逐步讀取法：迴圈搭配 `.append()` 逐列拼裝網格<br>9.2.3 巢狀列表生成式動態讀取神技：一行取代 4 行的高效寫法<br>9.2.4 二維解包優雅輸出：`for row in grid: print(*row)`<br>9.2.5 整齊對齊格式化輸出：`f"{val:4d}"` 打造視覺化工整矩陣<br>9.2.6 字元網格字串輸入：`[list(input().strip()) for _ in range(R)]`（迷宮地圖讀入） | 第 4 章（第 11 頁）、第 13 章（第 47 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 9.3 二維陣列初始化與參照共用致命陷阱 | 深度剖析 `[[0]*C for _ in range(R)]` 正確建構法與 `id()` 驗證，警惕淺拷貝慘劇；實作布林拜訪標記矩陣、座標規律填值與獨立深拷貝。 | 9.3.1 致命陷阱重現：`[[0] * C] * R` 淺拷貝參照共用慘劇<br>9.3.2 正確初始化黃金法則：`[[0] * C for _ in range(R)]`<br>9.3.3 記憶體指標探測：用 `id()` 驗證每列記憶體獨立性<br>9.3.4 布林標記地圖與字元地圖初始化：`False` 拜訪矩陣與符號地圖<br>9.3.5 動態網格生成：依座標規律填入初始數值（如 `r * C + c + 1`）<br>9.3.6 二維陣列的獨立深拷貝：`[row[:] for row in grid]` | 第 13 章（第 47 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 9.4 二維網格雙重走訪與行列統計 | 熟練雙重迴圈 `for r ... for c ...` 時鐘模型；實作橫列加總 `sum(row)` 與直行固定走訪加總 `grid[r][c]`，全網格搜尋極值與條件計數。 | 9.4.1 雙重迴圈時鐘模型：外層列 $r$ 與內層行 $c$ 的時序與座標對生成<br>9.4.2 橫列直接走訪：`for row in grid:` 搭配 `sum(row)` 極速列統計<br>9.4.3 橫列索引走訪：`for r in range(R):` 的帶編號列統計與報表產出<br>9.4.4 直行（直欄）維度翻轉統計：外層 $c$ 內層 $r$ 的縱向走訪累加<br>9.4.5 全網格全域極值搜尋與座標定位鎖定<br>9.4.6 網格條件計數與布林走訪過濾 | 第 13 章（第 47–48 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 9.5 二維方陣與特殊走訪：對角線與棋盤規律 | 獨立剖析主對角線（$r == c$）、副對角線（$r + c == N - 1$）、上三角/下三角走訪、黑白棋盤奇偶染色與中心點對稱統計。 | 9.5.1 主對角線走訪規律（$r == c$）<br>9.5.2 副對角線走訪規律（$r + c == N - 1$）<br>9.5.3 X 形交叉雙對角線與中心點重複防禦<br>9.5.4 上三角與下三角矩陣走訪<br>9.5.5 黑白棋盤格座標規律（$(r + c) \% 2$）<br>9.5.6 方陣對稱性檢驗（$grid[r][c] == grid[c][r]$） | 第 13 章（第 48 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 9.6 矩陣幾何操作與逆推還原（APCS b266 專題） | 掌握水平翻轉（左右對調）、垂直翻轉（上下對調 `grid[::-1]`）、非方陣轉置與順逆時針 90 度旋轉，貫通 APCS b266 逆操作還原實戰。 | 9.6.1 矩陣水平翻轉（Horizontal Flip / 左右對調）<br>9.6.2 矩陣垂直翻轉（Vertical Flip / 上下顛倒）<br>9.6.3 矩陣轉置操作（Matrix Transpose / 行列互換）<br>9.6.4 矩陣旋轉 90 度（順時針與逆時針）<br>9.6.5 連續操作與變數尺寸動態追蹤<br>9.6.6 APCS b266 矩陣轉換：逆操作還原心法（APCS 實戰原型） | 第 13 章 轉置矩陣與幾何變換（第 48–51 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 9.7 二維網格導航：方向向量與相鄰探測（APCS e287 原型） | 定義 `dr, dc` 方向差值陣列，結合合法邊界防護條件 `0 <= nr < R and 0 <= nc < C`，四方向/八方向相鄰探測，貪婪尋路與拜訪標記。 | 9.7.1 網格相鄰座標與相對位移（四方向上下左右）<br>9.7.2 方向向量差值陣列（Direction Vectors `dr, dc`）<br>9.7.3 合法邊界防護條件（Boundary Guard）<br>9.7.4 八方向相鄰探測（8-Neighbors / 踩地雷原理）<br>9.7.5 拜訪標記矩陣（Visited Grid）與貪婪前進<br>9.7.6 APCS e287 機器人的路徑：全圖貪婪尋路實戰（APCS 實戰原型） | 第 13 章（第 52 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 9.8 網格射線掃描（Raycasting）與連線阻擋（APCS g596 專題） | 解析射線連續推進 `while` 迴圈架構，撞牆與撞障礙物雙終止條件，四方向射線雷射掃描與骨牌連線染色模擬。 | 9.8.1 沿單一方向連續推進模型（步進 while 迴圈）<br>9.8.2 射線撞牆邊界終止條件（Boundary Stop）<br>9.8.3 射線遇障礙物阻擋機制（Obstacle Stop）<br>9.8.4 十字四方向雷射掃描器（Cross Laser Scanner）<br>9.8.5 雙柱連線與網格區間染色（Line Segment Marking）<br>9.8.6 APCS g596 骨牌遊戲：柱子拔除與連線阻擋動態模擬（APCS 實戰原型） | 第 13 章（第 52 頁）、第 15 章 g596（第 70–73 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 9.9 網格防護與動態模擬：哨兵加框與雙矩陣快照（APCS f313 專題） | 外圍哨兵免邊界判斷極速走訪，破解原地修改之資料污染陷阱，實作雙矩陣快照切換（Delta Grid）與人口擴散多回合模擬。 | 9.9.1 哨兵加框法概念（Grid Padding / 邊界外圍加保護層）<br>9.9.2 加框矩陣與原矩陣座標對應（$(r, c) \leftrightarrow (r+1, c+1)$）<br>9.9.3 原地修改的「資料污染」致命陷阱（In-place Modification Pitfall）<br>9.9.4 增減量矩陣架構（Delta Grid）<br>9.9.5 單回合相鄰擴散與增減量結算<br>9.9.6 APCS f313 人口遷移：多回合動態擴散模擬實戰（APCS 實戰原型） | 第 13 章 邊界加框法（第 51 頁）、第 15 章 f313（第 64–65 頁） | ✅ 已生成 | ⏳ 製作中 |
| **第十章 字典（Dictionary）、集合（Set）與元組（Tuple）的解題應用** | 10.1 元組特性、建立與不可變保護機制 | 從串列過渡至元組容器，解析不可變（Immutable）特性與單元素逗號語法，掌握正負索引、切片、型態互轉與多變數解包賦值。 | 10.1.1 為什麼需要不可變容器？（串列被誤改的慘劇 vs 元組的防禦屏障）<br>10.1.2 元組宣告語法：小括號 `(...)` 與單元素逗號陷阱 `(x,)`<br>10.1.3 正負索引存取與切片操作（承襲串列直覺，唯獨嚴禁賦值修改 `TypeError`）<br>10.1.4 型態互轉：`tuple(list)` 與 `list(tuple)` 的彈性切換<br>10.1.5 元組解包賦值（Tuple Unpacking）與多變數同時接收<br>10.1.6 二維座標封裝：以 `(r, c)` 元組表示平面網格點 | 第 17.1 節（第 79 頁） | ✅ 已生成 | ⏳ 製作中 |
|  | 10.2 元組進階操作與可雜湊特性（Hashable） | 深入記憶體位址解析可變與不可變物件本質，闡釋 Hashable 雜湊鍵值原理，實作結構化元組打包、座標位移向量與多準則評比排序。 | 10.2.1 可變（Mutable）與不可變（Immutable）物件的記憶體行為本質<br>10.2.2 什麼是「可雜湊（Hashable）」？為什麼只有不可變物件能被雜湊？<br>10.2.3 巢狀元組（Nested Tuples）與結構化多欄位資料打包（如 `(id, name, score)`）<br>10.2.4 元組在二維座標位移運算中的應用（`(r + dr, c + dc)`）<br>10.2.5 多傳回值模擬（為第 11 章自訂函式多值 return 提早鋪墊）<br>10.2.6 元組比大小與字典序排序原則（多準則評比原型） | 第 17.1 節（第 79 頁） | ✅ 已生成 | ⏳ 製作中 |
|  | 10.3 字典概念、建立與鍵值對存取（Key-Value） | 從數值索引邁向名牌索引，掌握大括號鍵值對語法 `{key: val}`、Key 的 Hashable 限制、KeyError 防範與就地增刪改查。 | 10.3.1 為什麼需要字典？從「數值索引」邁向「任意名牌索引」的認知躍升<br>10.3.2 大括號鍵值對語法 `{key: value}` 與空字典建立 `{}`<br>10.3.3 鍵的合法型態限制（Key 必須是 Hashable：整數、字串、Tuple，嚴禁 List）<br>10.3.4 鍵值存取 `d[key]` 與未定義鍵引發 `KeyError` 的致命陷阱<br>10.3.5 新增與修改鍵值對：`d[key] = new_val` 的就地更新機制<br>10.3.6 刪除鍵值對：`del d[key]` 與 `.pop(key, default)` 安全彈出 | 第 17.1 節 字典與雜湊（補充單元） | ✅ 已生成 | ⏳ 製作中 |
|  | 10.4 字典常用方法與走訪技巧 | 熟練 `d.get()` 安全取值、`in` 運算子 $O(1)$ 成員檢查，精通 keys()、values()、items() 走訪與字典生成式。 | 10.4.1 安全取值神器：`d.get(key, default)` 徹底杜絕 KeyError 崩潰<br>10.4.2 成員檢查：`key in d` 與 `key not in d` 的 $O(1)$ 極速存在性判定<br>10.4.3 鍵走訪：`for k in d:` 或 `d.keys()` 循序走訪<br>10.4.4 值走訪：`d.values()` 與極值/總和運算（`max(d.values())`, `sum(d.values())`）<br>10.4.5 鍵值對走訪：`for k, v in d.items():` 雙變數解包走訪<br>10.4.6 字典生成式（Dictionary Comprehension）：`{k: v for ... if ...}` | 第 17.1 節 字典與雜湊（補充單元） | ✅ 已生成 | ⏳ 製作中 |
|  | 10.5 字典在 APCS 的經典解題模式 | 破解陣列開太大引發的 MLE 困境，以字典實作動態頻率統計、字元/代碼查表、大值域離散化映射與分類分組桶（Bucket）。 | 10.5.1 頻率統計模式（Frequency Counter）：大數值與字串動態計數器<br>10.5.2 查表法（Lookup Table）：英文代碼與符號轉換（如羅馬數字、平仄對照）<br>10.5.3 離散化 / 座標壓縮（Coordinate Compression）：以字典建立排名映射<br>10.5.4 雙向映射表（Two-way Mapping）：Key 與 Value 互換技巧<br>10.5.5 分組聚合（Group By / Bucket）：以字典收集分類清單（`d[category].append(item)`）<br>10.5.6 字典與串列效能實測對比：$O(1)$ vs $O(N)$ 查詢速度震撼體驗 | 第 17.1 節 字典與雜湊（補充單元） | ✅ 已生成 | ⏳ 製作中 |
|  | 10.6 集合特性、建立與元素操作 | 剖析無序性與唯一性天然去重特性，破除 `set()` 與 `{}` 空集合宣告陷阱，掌握 `set(list)` 轉換、動態增刪與集合生成式。 | 10.6.1 集合概念：無序性（Unordered）與元素唯一性（Unique 天然去重神器）<br>10.6.2 集合建立語法與空集合陷阱：為什麼 `{}` 是字典，空集合必須寫 `set()`！<br>10.6.3 可迭代物件快速去重：`set(my_list)` 與去重後長度量測 `len(s)`<br>10.6.4 元素動態增刪：`.add(x)`、`.remove(x)` 與防報錯 `.discard(x)`<br>10.6.5 集合生成式（Set Comprehension）語法與動態過濾<br>10.6.6 集合元素限制：必須是可雜湊物件（可存 int, str, tuple，嚴禁放 list 或 set） | 第 17.1 節 集合(set)（第 78–79 頁） | ✅ 已生成 | ⏳ 製作中 |
|  | 10.7 集合代數運算子與范氏圖解題實戰 | 結合范氏圖幾何模型，詳解交集 `&`、聯集 <code>&#124;</code>、差集 `-`、對稱差 `^`，掌握黑名單過濾、獨家元素挑選與子集超集判斷。 | 10.7.1 范氏圖（Venn Diagram）幾何模型與集合四大運算<br>10.7.2 交集運算子 `&` 與方法 `intersection()`（共同名單 / 共同選課）<br>10.7.3 聯集運算子 `&#124;` 與方法 `union()`（全體名冊合併）<br>10.7.4 差集運算子 `-` 與方法 `difference()`（批量黑名單過濾）<br>10.7.5 對稱差運算子 `^` 與方法 `symmetric_difference()`（獨家元素篩選）<br>10.7.6 子集與超集判斷（`<=`, `>=`, `issubset()`, `issuperset()`） | 第 17.1 節 集合運算（第 79 頁） | ✅ 已生成 | ⏳ 製作中 |
|  | 10.8 雜湊容器綜合實戰：座標標記、稀疏網格與圖論前導 | 融會貫通元組、字典與集合，實作以 `(r, c)` 集合標記已拜訪點、以字典表示百萬級稀疏網格、模擬圖論鄰接串列與真題原型分析。 | 10.8.1 元組作為集合元素：以 `(r, c)` 集合實現稀疏地圖已拜訪標記（Visited Set）<br>10.8.2 元組作為字典鍵值：以 `{(r, c): value}` 實現動態稀疏網格（省去百萬記憶體）<br>10.8.3 字典模擬圖論鄰接串列（Adjacency List）：`graph[u] = [v1, v2]`<br>10.8.4 多鍵綜合排序：字典轉串列排序（`sorted(d.items(), key=...)`）<br>10.8.5 重複元素偵測與首個重複值定位（One-pass Hash Scan）<br>10.8.6 APCS 歷屆真題中的雜湊思維總覽（為第 14 章 g276 魔王迷宮、c291 小群體深度鋪墊） | 第 17.1 節（第 78–79 頁）、第 15 章真題預備 | ✅ 已生成 | ⏳ 製作中 |
| **第十一章 自訂函數（Function）與變數範圍（Scope）** | 11.1 函數定義（def）、呼叫流程與參數傳入基礎 | 建立黑盒子思維，掌握 def 宣告、呼叫執行時序、單參數與多參數位置對應，理解無回傳值函數預設產出 None 之本質。 | 11.1.1 為什麼需要函數？從重複代碼到模組化封裝（黑盒子概念）<br>11.1.2 函數的定義（`def`）、命名規範與呼叫（Call）語法<br>11.1.3 程式執行順序：定義期（Def Time）與呼叫期（Call Time）的追蹤<br>11.1.4 單一參數傳入與引數匹配：讓函數具備動態適應力<br>11.1.5 多參數位置對應（Positional Arguments）與逗號分隔<br>11.1.6 無回傳值函數的本質：動作型函數與預設回傳的 `None` | 第 14 章 控制程式流程—自訂函數（第 53 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 11.2 函數回傳值（return）、多值回傳與衛語句提早結束 | 徹底辨析 return 資料產出與 print 終端輸出的本質差異，運用衛語句進行提早攔截防禦，掌握多值打包為 Tuple 與呼叫端解包。 | 11.2.1 `return` 關鍵字：將函數計算結果產出並交接給外部變數<br>11.2.2 世紀迷思剖析：`return`（資料輸出）vs `print()`（終端列印）<br>11.2.3 衛語句（Guard Clauses）：利用提前 `return` 進行非法條件防禦攔截<br>11.2.4 多條件分支回傳：`if-elif-else` 中的 return 覆蓋完整性<br>11.2.5 函數多值回傳技巧：逗號自動打包為 Tuple 與呼叫端多變數解包<br>11.2.6 函數回傳布林值：構建高可讀性的判定函數（Predicate Functions） | 第 14 章（第 53–54 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 11.3 參數傳遞機制與副作用防禦（不可變物件 vs 可變物件串列/字典傳遞） | 剖析 Python 物件參照傳遞（Pass-by-assignment），對比不可變數值與可變串列/字典在函數內的行為差異，掌握 `.copy()` 防護與安全預設參數。 | 11.3.1 不可變物件傳遞（整數、字串、元組）：函數內部修改為何不影響外部？<br>11.3.2 可變物件傳遞（串列、字典）：內部呼叫方法導致外部同步變化的現象<br>11.3.3 賦值遮蔽 vs 就地修改：`arr = []`（重新指向）與 `arr.append()`（原處竄改）<br>11.3.4 防禦性複製：在函數內使用 `.copy()` 阻斷對外部原始陣列的副作用<br>11.3.5 參數預設值（Default Parameters）基本語法與呼叫端彈性<br>11.3.6 陷阱防範：避免使用可變物件（如 `[]`）作為預設參數的機制與正解（`None` 慣用法） | 第 14 章（第 53 頁）、第 17.3 節（第 80 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 11.4 變數作用域：區域變數（Local Scope）與同名變數遮蔽（Shadowing） | 說明函數內部獨立小王國之局部作用域與生命週期，解析全域與區域同名遮蔽現象，防範 UnboundLocalError 陷阱。 | 11.4.1 什麼是作用域？函數內部的獨立小王國（Local Scope）<br>11.4.2 區域變數的生命週期：呼叫時誕生、結束時釋放消亡<br>11.4.3 全域與區域同名：變數遮蔽（Shadowing）現象與就近原則<br>11.4.4 在函數內部「唯讀」存取全域變數的合法行為與邊界<br>11.4.5 經典報錯剖析：`UnboundLocalError: local variable referenced before assignment`<br>11.4.6 設計原則：為什麼盡量依賴「參數傳入、return 傳出」而非依賴外部全域變數？ | 第 14 章 全域變數(global)（第 54 頁） | ✅ 已生成 | ⏳ 製作中 |
| | 11.5 全域變數（global）在 APCS 競賽中的使用準則與除錯防禦 | 掌握 global 關鍵字在函數內改寫全域變數的機制，分析競賽中維護最佳解計數器與大型矩陣之實戰策略，落實多筆測資初始化重置防禦。 | 11.5.1 `global` 關鍵字：在區域內宣告並修改全域變數的值<br>11.5.2 APCS 競賽實戰場景一：全域最佳解累計計數器（`ans = min(ans, ...)`）<br>11.5.3 APCS 競賽實戰場景二：大型地圖/圖論鄰接矩陣的全域共享存取<br>11.5.4 可變物件免 `global` 原理：為何串列原處修改不需要宣告 `global`？<br>11.5.5 濫用 `global` 的隱患：狀態污染、跨測資未清空（重測致命傷）<br>11.5.6 競賽除錯守則：如何在多筆測資迴圈開頭正確重置（Reset）全域變數 | 第 14 章（第 54–55 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 11.6 線性遞迴（Linear Recursion）：呼叫堆疊（Call Stack）展開與終止條件 | 從單線推進切入遞迴思維，透視呼叫堆疊（Call Stack）推入與彈出過程，嚴格掌握 Base Case 煞車皮與階乘/反轉字串追蹤。 | 11.6.1 遞迴第一步：認識「函數呼叫自己」與鏡中鏡意象<br>11.6.2 呼叫堆疊（Call Stack）物理模型：棧針推入（Push）與彈出（Pop）過程<br>11.6.3 終止條件（Base Case）：遞迴能停下來的唯一煞車皮<br>11.6.4 經典範例：階乘累乘 $N!$ 與數列累加的呼叫與歸還路徑追蹤<br>11.6.5 字串與串列的線性遞迴：反轉字串與倒序輸出<br>11.6.6 遞迴深度限制與防爆：`RecursionError: maximum recursion depth exceeded` | 第 14 章 遞迴（第 55 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 11.7 樹狀分支遞迴（Tree Recursion）與數論演算法（費氏數列與輾轉相除法 GCD） | 突破單線思維掌握一分二之遞迴展開樹，剖析費氏數列重疊子問題，實作歐幾里得輾轉相除法 GCD 與快速冪遞迴版。 | 11.7.1 雙分支遞迴：一分二的遞迴樹（Recursion Tree）展開圖解<br>11.7.2 經典費氏數列（Fibonacci）：$F(n) = F(n-1) + F(n-2)$ 的樹狀執行流程<br>11.7.3 重疊子問題（Overlapping Subproblems）的感性認知與運算量爆炸分析<br>11.7.4 經典數論：輾轉相除法求最大公因數（GCD）之極簡遞迴實現<br>11.7.5 快速冪運算遞迴版：將 $O(N)$ 降至 $O(\log N)$ 的分治思維萌芽<br>11.7.6 遞迴思維總結：何時該用迴圈（迭代）？何時遞迴思維更直觀？ | 第 14 章（第 55 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 11.8 函數模組化解題實戰：Top-Down 拆題法與競賽輔助函數庫建立 | 融會貫通函數語法，運用自頂向下（Top-Down）拆題法，構建二維網格安全邊界檢查、曼哈頓距離與質數判定等必備解題工具庫。 | 11.8.1 自頂向下拆題思維（Top-Down Decomposition）：先寫主流程骨架，再填細節函數<br>11.8.2 必備輔助函式庫一：二維網格安全邊界檢查 `in_bound(r, c, H, W)`<br>11.8.3 必備輔助函式庫二：幾何與網格曼哈頓距離 / 歐式距離計算函數<br>11.8.4 必備輔助函式庫三：數論判定工具（質數判定、因數個數、迴文數判定）<br>11.8.5 實戰綜合演練：模擬題實戰（多階段邏輯透過函數拆解，程式碼降至 30 行以內）<br>11.8.6 模組化除錯策略：單獨測試子函數（Unit Test 雛形）快速定位 Bug | 第 14 章（第 53–55 頁）、第 15 章真題預備 | ⏳ 製作中 | ⏳ 製作中 |
| **第十二章 排序、搜尋演算法與相關模組** | 12.1 就地排序（sort）與新物件排序（sorted） | 比較 `a.sort()`（直接修改原物件）與 `sorted(a)`（產生排序新物件）之語意與使用時機。 | 12.1.1 排序核心概念與升序預設行為<br>12.1.2 原地修改方法 `list.sort()`：修改原串列且回傳值為 None<br>12.1.3 內建函數 `sorted(iterable)`：保留原物件並產生全新排序串列<br>12.1.4 記憶體耗損與使用時機對比：何時該用 sort、何時該用 sorted<br>12.1.5 非串列序列的排序：以 sorted() 對字串與集合排序<br>12.1.6 穩定排序（Stable Sort）特性：相等元素之原始相對順序保留 | 第 4 章（第 13 頁）、第 16 章 排序與搜尋（第 74 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 12.2 降序排序與字串字典序原則 | 說明 `reverse=True` 逆序排列，以及文字依 ASCII 順序比對之字典序規則。 | 12.2.1 參數 `reverse=True`：從大到小反向排序<br>12.2.2 數值排序 vs 字串字典序（Lexicographical Order）之本質差異<br>12.2.3 字典序陷阱：字串 `'10'` 小於 `'2'` 的成因與數值轉型必要性<br>12.2.4 大小寫混合字串排序與 ASCII 編碼權重比對<br>12.2.5 負號取反技巧：利用 `-x` 進行升降序快速切換<br>12.2.6 多條件排序入門：首要條件升序、次要條件降序初探 | 第 16 章（第 74 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 12.3 自訂排序鍵值：sort 搭配 lambda | 使用 `key=lambda x: ...` 指定特定欄位（如二維陣列第 2 欄）或欄位運算總和進行客製化排序。 | 12.3.1 參數 `key=` 概念：自訂特徵投影函數<br>12.3.2 匿名函數 `lambda` 語法：一分鐘快速掌握 `lambda x: ...`<br>12.3.3 二維串列自訂欄位排序：依第二欄成績 `key=lambda x: x[1]` 排序<br>12.3.4 多準則雙鍵值排序：依成績降序、學號升序 `key=lambda x: (-x[1], x[0])`<br>12.3.5 依字串長度排序：`key=len` 與自訂計算維度排序<br>12.3.6 APCS 考場最愛：座標平面點排序（依距離原點平方和排序） | 第 16 章（第 74–75 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 12.4 線性搜尋與 index() 例外防範 | 說明無順序資料逐一比對機制，並分析 `index()` 找不到數值時引發 `ValueError` 之防範。 | 12.4.1 線性搜尋（Linear Search）原理：無序資料的地毯式逐一走訪<br>12.4.2 時間複雜度 $O(N)$ 分析與最差情況考量<br>12.4.3 `list.index(x)` 方法與找不到元素時引發 `ValueError` 的致命陷阱<br>12.4.4 成員檢查防禦：以 `if x in a:` 搭配 `a.index(x)` 確保安全查詢<br>12.4.5 手刻線性搜尋：回傳第一個匹配索引與找不到時回傳 -1 的慣例<br>12.4.6 多重目標搜尋：收集所有匹配索引清單 | 第 16 章 線性搜尋（第 75 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 12.5 二分搜尋法手刻演算法 | 針對已排序串列實作雙指標折半搜尋區間演算法，達成 $O(\log n)$ 極速查詢。 | 12.5.1 二分搜尋前提鐵律：資料必須已經排序！<br>12.5.2 猜數字遊戲模型：雙指標 `left, right` 與折半搜尋區間<br>12.5.3 中間索引計算與溢位防範：`mid = (left + right) // 2`<br>12.5.4 區間縮小邏輯：`left = mid + 1` 與 `right = mid - 1`<br>12.5.5 時間複雜度 $O(\log N)$ 的爆炸性威力（百萬筆資料只需 20 次比對）<br>12.5.6 手刻二分搜尋完整實作與找不到時的邊界回傳值 | 第 16 章 二分搜尋（第 75–76 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 12.6 內建二分搜尋模組：bisect | 引入標準庫 `import bisect`，調用 `bisect.bisect_left()` 達成高度優化之二分搜尋。 | 12.6.1 標準函式庫 `import bisect` 模組導引與底層 C 優化優勢<br>12.6.2 `bisect.bisect_left()`：尋找第一個大於或等於目標之插入位置<br>12.6.3 `bisect.bisect_right()`：尋找大於目標之插入位置<br>12.6.4 區間數值分級神器：利用 bisect 快速實作等第成績評定<br>12.6.5 維護已排序序列：使用 `bisect.insort()` 進行高效有序插入<br>12.6.6 APCS 實戰：利用 bisect 解決單調函數極值與數值範圍計數問題 | 第 16 章（第 76 頁） | ⏳ 製作中 | ⏳ 製作中 |
| **第十三章 程式除錯（Debug）與異常處理** | 13.1 競技程式評判結果型別解析 | 詳細解析線上評判常見結果：`AC`（通過）、`CE`（語法錯）、`WA`（答案錯）、`TLE`（超時）、`RE`（執行時期錯誤）。 | 13.1.1 線上評判系統（Online Judge）自動裁判運作架構<br>13.1.2 `AC`（Accepted）：全數測資完全通過之評斷標準<br>13.1.3 `CE`（Compile Error / SyntaxError）：程式碼語法不合規解析<br>13.1.4 `WA`（Wrong Answer）：輸出答案或格式與標準測資不符分析<br>13.1.5 `TLE`（Time Limit Exceeded）：演算法複雜度過高或無窮迴圈診斷<br>13.1.6 `RE`（Runtime Error）：IndexError、ZeroDivisionError 等執行崩潰分析 | 第 0 章 基本知識（第 1–2 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 13.2 語法錯誤與語意錯誤區別 | 比較直譯器可報錯的語法問題（CE），與語法完全合法但運算邏輯不符題意之語意問題（WA）。 | 13.2.1 語法錯誤（Syntax Errors）：直譯器拒絕啟動的結構性硬傷<br>13.2.2 常見語法雷區：漏冒號、括號不對稱、縮排不一致與關鍵字拼錯<br>13.2.3 執行時期錯誤（Exceptions）：執行中途突發崩潰（除以零、型態錯）<br>13.2.4 語意錯誤（Logic / Semantic Errors）：程式正常執行但結果錯誤<br>13.2.5 邊界偏差錯誤（Off-by-one Error）：迴圈多跑或少跑一回合之辨識<br>13.2.6 運算優先級引發的隱形邏輯錯誤排查 | 第 0 章（第 1 頁）、第 6 章（第 16 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 13.3 核心除錯策略：錯誤重現、故障隔離與 print 追蹤 | 講述運用固定測資使錯誤重現、故障隔離法，並以 `print()` 逐點追蹤重要變數值以定位臭蟲。 | 13.3.1 除錯第一心法：建構極小可重現測試案例（Minimal Reproducible Example）<br>13.3.2 故障隔離法（Divide and Conquer）：二分註解程式碼鎖定臭蟲行<br>13.3.3 print 追蹤法：在關鍵節點印出變數中間狀態與迴圈次數<br>13.3.4 印出格式技巧：標籤化印出 `print(f'[DEBUG] r={r}, val={val}')`<br>13.3.5 送出前的致命警戒：務必刪除或註解所有 Debug 用的 print 語句<br>13.3.6 極端邊界測資清單建立（空、0、負數、極大值、只有單一元素） | 第 6 章 碰到錯誤怎麼辦 debug（第 16 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 13.4 例外捕捉語法：try ... except 架構 | 說明使用 `try: ... except: ...` 防禦性處理執行時期異常，避免程式崩潰中斷。 | 13.4.1 防禦性程式設計理念：優雅處理非預期異常而不崩潰<br>13.4.2 `try ... except` 基本結構與執行流程跳轉<br>13.4.3 捕捉特定異常：`ValueError`, `ZeroDivisionError`, `IndexError`<br>13.4.4 APCS 未知行數終極法寶：`try ... except EOFError:` 讀到檔案結尾<br>13.4.5 `else` 與 `finally` 區塊角色與資源釋放概念<br>13.4.6 競技程式中的例外使用準則：切勿過度滥用 except 遮掩邏輯錯誤 | 第 17.1 節（第 78 頁） | ⏳ 製作中 | ⏳ 製作中 |
| **第十四章 APCS 實作真題循序漸進解題特訓** | 14.1 c294. 三角形辨別（APCS 2016-10 實作第 1 題） | 【題意分析】給定三邊長排序並判斷直角/鈍角/銳角。<br>【關鍵技巧】三數極值運算排序、畢氏定理平方和比對、互斥分支判斷。<br>【實戰訓練】引導學生逐步組裝並獨立寫出完整程式。 | 14.1.1 題意解析與三角形形成條件判定<br>14.1.2 三邊長排序策略（直接比大小 vs 串列排序）<br>14.1.3 畢氏定理平方和幾何模型與銳鈍直角分類<br>14.1.4 輸出順序與多分支互斥條件防禦<br>14.1.5 完整 AC 程式碼實作與格式化輸出<br>14.1.6 複雜度分析與常見 WA 陷阱（非三角形優先判斷） | 第 11 章 c294（第 34 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.2 c290. 秘密差（APCS 2017-03 實作第 1 題） | 【題意分析】輸入超長數字，計算奇數位數字和與偶數位數字和的差值。<br>【關鍵技巧】大數以字串形式讀取、字串索引走訪、`int()` 轉型累加、`abs()` 絕對值。<br>【實戰訓練】引導學生完成字串分解並獨立撰寫 AC 程式。 | 14.2.1 題意解析與 1000 位超長大整數挑戰<br>14.2.2 大數以字串形式讀入與記憶體防爆<br>14.2.3 字串索引走訪與奇數位/偶數位區分<br>14.2.4 字元轉整數 `int()` 與雙計數器累加<br>14.2.5 `abs()` 絕對值差值計算與完整 AC 實作<br>14.2.6 1-based 位數 vs 0-based 索引之對齊陷阱 | 第 11 章 c290（第 35–37 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.3 c461. 邏輯運算子（APCS 2017-10 實作第 1 題） | 【題意分析】給定兩輸入值與一輸出值，反查可能為 AND/OR/XOR 何種運算。<br>【關鍵技巧】非零轉布林真值、邏輯運算子比對、旗標變數與無解（IMPOSSIBLE）防禦。<br>【實戰訓練】學生自主梳理真值邏輯並寫出完整分支判定。 | 14.3.1 題意解析：依真值反推可能的邏輯運算子<br>14.3.2 非零整數轉布林真值（`x != 0`）之轉換邏輯<br>14.3.3 AND, OR, XOR 運算子真值表對齊比對<br>14.3.4 旗標變數維護與多解依序輸出順序<br>14.3.5 無解狀態防護（輸出 IMPOSSIBLE）與完整 AC 程式<br>14.3.6 常見錯誤：整數 0/非0 混淆與優先級漏括號 | 第 11 章 c461（第 37–38 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.4 e286. 籃球比賽（APCS 2019-06 實作第 1 題） | 【題意分析】讀入兩場比賽各四節分數，判斷主隊或客隊是否連勝或一勝一負。<br>【關鍵技巧】多行輸入連續讀取、串列加總 `sum()`、分場計分比較與格式化輸出。<br>【實戰訓練】逐步引導四行輸入讀取架構，最後由學生自主完成比分判別。 | 14.4.1 題意解析與多行固定筆數讀取規格<br>14.4.2 連續 4 行分數輸入與整數映射<br>14.4.3 `sum()` 快速計算主隊與客隊總得分<br>14.4.4 格式化輸出比分（`主隊:客隊`）<br>14.4.5 兩場勝負統計與 Win/Lose/Tie 判定實作<br>14.4.6 多行讀取時序錯位與平手特例防護 | 第 11 章 e286（第 38–39 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.5 f579. 購物車（APCS 2020-07 實作第 1 題） | 【題意分析】模擬購物車購買（正數）與退貨（負數），統計同時包含特定兩項商品之顧客數。<br>【關鍵技巧】以 0 結尾的未知長度輸入處理、串列元素計數、多條件判斷。<br>【實戰訓練】拆解購物車抵銷統計流程，引導學生完成顧客清單判定。 | 14.5.1 題意解析：正數購買與負數退貨之抵銷機制<br>14.5.2 未知長度以 0 結尾的輸入串列解析<br>14.5.3 統計各特定商品最終淨購買量（正負抵銷）<br>14.5.4 同時包含商品 A 與 B 的雙條件過濾<br>14.5.5 顧客總人數計數器累加與完整 AC 實作<br>14.5.6 常見陷阱：購買後全數退貨（淨數量為0）之判斷 | 第 11 章 f579（第 39–40 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.6 f312. 人力分配（APCS 2020-10 實作第 1 題） | 【題意分析】兩座工廠依二次方程式獲利，將 $n$ 名員工分配至兩工廠求最大總利潤。<br>【關鍵技巧】二次函數公式計算、for 迴圈完整枚舉所有分配組合、極值更新 `max()`。<br>【實戰訓練】引導窮舉區間邊界與極值初值設定，學生獨立寫出枚舉程式碼。 | 14.6.1 題意解析：將 n 名員工分配給兩工廠求最大利潤<br>14.6.2 二次函數收益公式計算：$a x^2 + b x + c$<br>14.6.3 for 迴圈完整枚舉分配人數 $(x_1, x_2)$ 滿足 $x_1 + x_2 = n$<br>14.6.4 最大值變數初始化（初始值設為極小負數防護）<br>14.6.5 迴圈內動態更新最大總收益與完整 AC 程式<br>14.6.6 複雜度分析：$O(n)$ 極速枚舉與負收益陷阱 | 第 11 章 f312（第 40–41 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.7 f605. 購買力（APCS 2021-01 實作第 1 題） | 【題意分析】給定多筆三項商品價格，挑選極端價差大於門檻且平均低於預算者，統計總件數與花費。<br>【關鍵技巧】單行三數輸入、全距運算 `max - min`、整數除法平均、計數器與累加器更新。<br>【實戰訓練】引導拆解單筆過濾條件，學生獨立組裝迴圈與累加輸出。 | 14.7.1 題意解析：商品價格波動全距與平均預算過濾<br>14.7.2 單行三數讀取與極值計算（`max - min >= d`）<br>14.7.3 整數除法平均值計算：`sum // 3`<br>14.7.4 雙計數器模式：購買總件數與花費總金額<br>14.7.5 逐筆處理與多行輸入迴圈完整 AC 實作<br>14.7.6 一件都沒買（件數為 0、花費為 0）之邊界檢驗 | 第 11 章 f605（第 41 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.8 g275. 七言對聯（APCS 2021-09 實作第 1 題） | 【題意分析】給定多組上下聯七言聲調，檢驗二四六聲調相間、二七同句相反、末字平仄三原則。<br>【關鍵技巧】一維串列索引對位比對、布林旗標變數逐項防禦、違規代碼（A, B, C）組合印出。<br>【實戰訓練】逐條拆解對聯規則對位公式，學生獨立編寫規則判定並通過測資。 | 14.8.1 題意解析：上下聯平仄規則全景對齊<br>14.8.2 規則一：二四六聲調相間與句內交替比對<br>14.8.3 規則二：二七同句相反與跨句對立比對<br>14.8.4 規則三：末字平仄（上聯仄聲1、下聯平聲0）<br>14.8.5 違規代碼 A/B/C 收集與全合格輸出 None<br>14.8.6 0-based 索引轉換（第 2、4、6、7 字對應索引 1,3,5,6）防護 | 第 11 章 g275（第 41–42 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.9 g595. 修補圍籬（APCS 2021-11 實作第 1 題） | 【題意分析】修補損壞高度為 0 的圍籬，其修復高度為相鄰兩側非零高度之較小值，求總花費。<br>【關鍵技巧】一維串列相鄰走訪、左右兩端邊界特例防護（或前後加框技巧）、`min()` 累加。<br>【實戰訓練】引導學生繪製邊界圖並處理頭尾特例，最後自主寫出完整修補程式。 | 14.9.1 題意解析：損壞高度 0 圍籬修復成本最小化<br>14.9.2 一維串列相鄰探測與非零高度提取<br>14.9.3 邊界特例分析：最左端第 0 格與最右端最後一格<br>14.9.4 中間損壞格取相鄰兩側較小值：`min(left, right)`<br>14.9.5 總花費累加器與完整 AC 程式碼實作<br>14.9.6 加框技巧延伸：前後各補虛擬圍籬免邊界特判 | 第 11 章 g595（第 42–43 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.10 b266. 矩陣轉換（APCS 2016-03 實作第 2 題） | 【題意分析】給定最終矩陣與一連串翻轉、旋轉操作，逆推求出原始矩陣。<br>【關鍵技巧】二維陣列維護、逆序操作讀取（`reversed()`）、旋轉 90 度與上下翻轉矩陣轉換函數。<br>【實戰訓練】引導矩陣座標逆推變換公式，學生模組化撰寫轉換函數與主程式。 | 14.10.1 題意解析：給定最終矩陣逆推原始矩陣<br>14.10.2 逆向思維：操作序列倒序執行（`ops[::-1]`）<br>14.10.3 垂直翻轉之自逆性操作（`grid[::-1]`）<br>14.10.4 順時針旋轉之逆操作：逆時針旋轉 90 度<br>14.10.5 旋轉後的矩陣尺寸動態更新與全流程 AC 實作<br>14.10.6 連續旋轉與翻轉之動態記憶體配置優化 | 第 15 章 b266（第 56–58 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.11 c291. 小群體（APCS 2017-03 實作第 2 題） | 【題意分析】$N$ 個人互相指名好友，找出形成封閉循環的獨立群體總數量。<br>【關鍵技巧】一維串列模擬有向圖、已走訪標記陣列（visited）、while 迴圈追蹤循環軌跡。<br>【實戰訓練】引導圖論連通環概念，逐步構建標記機制，學生獨立寫出群體計數程式。 | 14.11.1 題意解析：好朋友指涉關係與獨立朋友圈個數<br>14.11.2 有向圖環狀模型與群體追蹤機制<br>14.11.3 布林拜訪陣列 `visited` 防止重複計數<br>14.11.4 while 迴圈循線追蹤好友鏈直到形成封閉環<br>14.11.5 全體成員枚舉、群體總數計數與完整 AC 程式<br>14.11.6 $O(N)$ 線性時間複雜度分析與孤立自環特例 | 第 15 章 c291（第 58–59 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.12 c295. 最大和（APCS 2016-10 實作第 2 題） | 【題意分析】從 $N$ 群數字每群挑出最大值求和 $S$，再篩選出能整除 $S$ 的被選數。<br>【關鍵技巧】二維網格走訪、每行取 `max()`、總和 `sum()`、列表生成式篩選因數與無因數（-1）輸出。<br>【實戰訓練】逐步引導分階段處理架構，學生自行撰寫出全功能過關代碼。 | 14.12.1 題意解析：每群挑選最大值求總和，並找出可整除總和之群極值<br>14.12.2 二維列讀取與每橫列 `max()` 極值提取<br>14.12.3 全群極值總和計算與第一行輸出<br>14.12.4 整除判定條件：`total % val == 0`<br>14.12.5 符合條件清單輸出與無解輸出 -1 之防禦<br>14.12.6 空格隔開輸出排版技巧與常見 PE/WA 陷阱 | 第 15 章 c295（第 59–60 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.13 c462. 交錯字串（APCS 2017-10 實作第 2 題） | 【題意分析】找出最長由連續 $k$ 個大寫與連續 $k$ 個小寫交錯組成之子字串長度。<br>【關鍵技巧】大小寫判別、長度分段壓縮編碼、滑動窗口連續交錯段統計、最大值維護。<br>【實戰訓練】引導大小寫連續段的統計轉換技巧，學生自主撰寫滑動計數邏輯。 | 14.13.1 題意解析：大小寫交錯且每段長度剛好為 k 之最長子字串<br>14.13.2 字元大小寫屬性判定（`isupper()` / `islower()`）<br>14.13.3 連續相同大小寫區塊長度編碼（Run-Length Encoding）<br>14.13.4 連續 k 長度區塊滑動視窗走訪<br>14.13.5 左右兩端不滿 k 但大於 k 的截斷銜接分析<br>14.13.6 最長合法長度更新與完整 AC 程式碼實作 | 第 15 章 c462（第 60–62 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.14 e287. 機器人的路徑（APCS 2019-06 實作第 2 題） | 【題意分析】機器人從全地圖最小數值起點出發，每步往相鄰未走過之最小格移動，直到無路可走。<br>【關鍵技巧】二維矩陣全局搜尋起點、四方向向量走訪、已拜訪標記、貪婪規則判定與路徑總和。<br>【實戰訓練】拆解網格走訪狀態機，學生獨立撰寫四方向探測與迴圈終止條件。 | 14.14.1 題意解析：全圖極小起點出發與相鄰貪婪尋路<br>14.14.2 全網格搜尋鎖定最小值座標 `(min_r, min_c)`<br>14.14.3 二維布林拜訪矩陣 `visited` 避免走回頭路<br>14.14.4 四方向向量探測：合法、未拜訪且數值最小鄰居<br>14.14.5 無路可走停機 break 與路徑點數總和累加實作<br>14.14.6 邊界防護與相鄰多個相等最小值時的題目保證驗證 | 第 15 章 e287（第 62–64 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.15 f313. 人口遷移（APCS 2020-10 實作第 2 題） | 【題意分析】城市網格中人口依相鄰區域每回合擴散，模擬 $k$ 天後求最大與最小人口數。<br>【關鍵技巧】二維陣列哨兵加框（-1 邊界）、雙矩陣狀態交替快照、整數除法擴散計算、極值統計。<br>【實戰訓練】引導狀態擴散暫存與同步更新機制，學生自主完成多輪模擬代碼。 | 14.15.1 題意解析：多城鎮四方向同步擴散與不可居住空地<br>14.15.2 原地修改資料污染陷阱與 Delta 增減量矩陣設計<br>14.15.3 有效相鄰城鎮探測與流出量計算（`val // k`）<br>14.15.4 m 回合動態推進主迴圈與 Delta 結算<br>14.15.5 排除 -1 障礙物搜尋全圖最小人口與最大人口<br>14.15.6 大地圖效能優化與零流出城鎮提前跳過技巧 | 第 15 章 f313（第 64–65 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.16 f580. 骰子（APCS 2020-07 實作第 2 題） | 【題意分析】多顆立體骰子依指令向前向右翻轉或兩兩交換，模擬最終所有骰子朝上點數。<br>【關鍵技巧】六面骰狀態資料結構設計（前後左右上下）、空間旋轉映射函數、串列位置交換。<br>【實戰訓練】引導骰子翻轉面值對應表，學生自訂翻轉函數並完成多骰子模擬。 | 14.16.1 題意解析：多顆骰子空間旋轉與兩顆骰子對調<br>14.16.2 骰子 6 面相對方位表示法（頂、底、前、後、左、右）<br>14.16.3 向前旋轉與向右旋轉之 4 面輪替映射推導<br>14.16.4 操作代碼解析：-1 向前、-2 向右、大於 0 交換兩骰<br>14.16.5 骰子陣列狀態更新與各骰頂面點數依序輸出<br>14.16.6 初始點數配置（上1下6、前4後3、左5右2）與對立面恆等於7 | 第 15 章 f580（第 65–66 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.17 f606. 流量（APCS 2021-01 實作第 2 題） | 【題意分析】多個伺服器流量分流至各基地台，依據各基地台容量門檻計算非線性費用，求最佳配置。<br>【關鍵技巧】二維流量矩陣轉換、分流累加計算、階梯式費率公式、最小值最佳化挑選。<br>【實戰訓練】引導矩陣相乘與聚合概念，學生自組成本計算式並完成競賽代碼。 | 14.17.1 題意解析：伺服器群網路流量分流與各方案費用計算<br>14.17.2 二維流量矩陣 $Q[i][j]$ 與伺服器指派陣列對應<br>14.17.3 站點間流量聚合：同機房內部流量 vs 跨機房外部流量<br>14.17.4 計費規則套用：內部流量小額計費、外部流量大額計費<br>14.17.5 多種分流方案枚舉與最低總營運花費更新<br>14.17.6 多維陣列轉置聚合與整數運算效能分析 | 第 15 章 f606（第 66–67 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.18 g276. 魔王迷宮（APCS 2021-09 實作第 2 題） | 【題意分析】多個魔王在網格中移動並留下炸彈，踩到炸彈則爆炸引爆魔王消失，求最終殘留炸彈數。<br>【關鍵技巧】集合 `set` 存放座標元組 `(r, c)`、出界判定、差集運算批量引爆、多物件移動模擬。<br>【實戰訓練】引導集合去重與差集移除的極速應用，學生自主完成遊戲模擬回圈。 | 14.18.1 題意解析：多隻魔王移動路徑、遺留炸彈與引爆連鎖<br>14.18.2 魔王位置與速度向量 $(r, c, dr, dc)$ 結構維護<br>14.18.3 放置炸彈與移動推進：出界魔王永久移除判定<br>14.18.4 踩雷陣亡檢驗：踩中炸彈之魔王與被踩炸彈同時爆炸消除<br>14.18.5 模擬迴圈直到所有魔王全數出界或陣亡<br>14.18.6 結算全地圖殘留未引爆炸彈總數與完整 AC 實作 | 第 15 章 g276（第 67–70 頁） | ⏳ 製作中 | ⏳ 製作中 |
| | 14.19 g596. 動線安排（APCS 2021-11 實作第 2 題） | 【題意分析】在網格中放置或拔除骨牌，相鄰骨牌間會連線拉出射線，模擬多回合操作求最終涵蓋格數。<br>【關鍵技巧】外圍哨兵加框、四方向射線延伸探測、全域變數 `global` 模組化、連鎖移除模擬。<br>【實戰訓練】拆解連線與拔除的射線雙向探測演算法，學生獨立完成高分大題。 | 14.19.1 題意解析：網格動態立柱、拔柱與骨牌直線連線<br>14.19.2 四方向射線掃描（Raycasting）尋找最近柱子<br>14.19.3 距離限制 $K$ 與中間無障礙阻擋之連線確立條件<br>14.19.4 拔柱時連線瓦解問題與全量射線重繪覆蓋架構<br>14.19.5 覆蓋格數統計與歷程最大覆蓋值追蹤<br>14.19.6 大測資效能分析與無函式純迴圈架構之 AC 驗證 | 第 15 章 g596（第 70–73 頁） | ⏳ 製作中 | ⏳ 製作中 |
